export enum PostType {
  Lost = 'Lost',
  Found = 'Found',
}

export enum PostStatus {
  Lost = 'Lost',
  Found = 'Found',
  Resolved = 'Resolved',
}

export enum PostMatchingStatus {
  Pending = 'Pending',
  Processing = 'Processing',
  Ready = 'Ready',
  Failed = 'Failed',
}
