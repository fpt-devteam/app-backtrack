export type GoogleMapFormattedLocation = {
  latitude: number;
  longitude: number;

  formattedAddress: string;
  placeId: string;
};

export type LocationCoordinates = {
  latitude: number;
  longitude: number;
};